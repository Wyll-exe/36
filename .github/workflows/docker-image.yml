name: Build & Push Docker Image

on: 
  [workflow_dispatch]
    
jobs:
    publish:
      runs-on: ubuntu-latest
      steps:
        - name: checkout
          uses: actions/checkout@v4
        - name: build image
          run: docker build -t test-cicd
        - name: push image
          run: |
            docker push test-cicd